// src/modules/fees/fees.labels.js
// Preset fee labels offered by the system; admins can also add custom labels.
const PRESET_FEE_LABELS = ['PTA', 'Printing', 'Mock'];

module.exports = { PRESET_FEE_LABELS };
